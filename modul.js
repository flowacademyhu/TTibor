const osszead = (a, b) => {
    return a + b;
};

const kivon = (a, b) => {
    return a*b;
};

const hatvany = (a, b) => {
    return Math.pow(a, b);
};

module.exports = {
    osszead,
    kivon,
    hatvany,
}

